import React from 'react';
import {Container} from "react-bootstrap";

const Auth = () => {
    return (
        <Container>
            <h1>Auth</h1>
        </Container>
    );
};

export default Auth;